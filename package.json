{
  "name": "youtube-transcript-batch-mac",
  "version": "1.0.0",
  "private": true,
  "description": "Electron desktop app for batch extracting YouTube transcripts on macOS.",
  "main": "dist/main.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "npm run build && electron dist/main.js",
    "test": "npm run build && node --test test/*.mjs"
  },
  "dependencies": {
    "electron": "^42.2.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
